#ifndef MYMATH_H
#define MYMATH_H

#define Min(x, y)           (x) < (y) ? (x) : (y)                   /* find the smaller one between x and y                     */
#define Max(x, y)           (x) > (y) ? (x) : (y)                   /* find the bigger one between x and y                      */

#endif // MYMATH_H
